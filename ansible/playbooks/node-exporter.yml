---
- name: Install Node Exporter
  hosts: all
  become: true

  tasks:

    - name: Install Node Exporter
      apt:
        name: prometheus-node-exporter
        state: present
        update_cache: true

    - name: Enable and start Node Exporter
      systemd:
        name: prometheus-node-exporter
        enabled: true
        state: started
